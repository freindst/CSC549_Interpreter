remember int a = 5;
remember int b = 5;
remember int c = 0;
while test a > 0 do begin print a, update b = 5, while test b > 0 do begin print b, update c = do-math c + 1, update b = do-math b - 1 end, update a = do-math a - 1 end;